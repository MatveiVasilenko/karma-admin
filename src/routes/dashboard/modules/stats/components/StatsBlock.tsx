import React, { FC } from 'react';

interface IStatsBlockProps {
  
};

const StatsBlock:FC<IStatsBlockProps> = ({}) => {
    return (
        <div>StatsBlock</div>
    )
};

export default StatsBlock;