import React, { FC } from 'react';

interface IFilterGraficProps {
  
};

const FilterGrafic:FC<IFilterGraficProps> = ({}) => {
    return (
        <div>Filter</div>
    )
};

export default FilterGrafic;